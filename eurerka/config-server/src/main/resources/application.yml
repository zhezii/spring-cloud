spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhezii/SpringcloudConfig/
          search-paths: respo
          username: zhezii
          password: ddhjx125
      label: master
server:
  port: 8888


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/

