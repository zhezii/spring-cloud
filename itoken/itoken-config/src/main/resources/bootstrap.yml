spring:
  application:
    name: itoken-config
  boot:
    admin:
      client:
        url: http://localhost:8004
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/zhezii/spring-cloud-config.git
          search-paths: respo
          username: zhezii
          password: ddhjx125


  zipkin:
    base-url: http://localhost:9411

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,info